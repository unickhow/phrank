import type { Component } from 'solid-js';
import FormCard from './components/FormCard';

const App: Component = () => {
  return (
    <main class="h-screen">
      <FormCard />
    </main>
  );
};

export default App;
